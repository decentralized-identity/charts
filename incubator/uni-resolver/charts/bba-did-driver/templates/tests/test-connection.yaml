apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bba-did-driver.fullname" . }}-test-connection"
  labels:
    {{- include "bba-did-driver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bba-did-driver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
