apiVersion: v1
kind: Service
metadata:
  name: {{ include "bba-did-driver.fullname" . }}
  labels:
    {{- include "bba-did-driver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bba-did-driver.selectorLabels" . | nindent 4 }}
