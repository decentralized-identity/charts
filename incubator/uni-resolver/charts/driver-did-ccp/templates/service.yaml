apiVersion: v1
kind: Service
metadata:
  name: {{ include "driver-did-ccp.fullname" . }}
  labels:
    {{- include "driver-did-ccp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "driver-did-ccp.selectorLabels" . | nindent 4 }}
