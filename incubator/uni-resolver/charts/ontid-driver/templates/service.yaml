apiVersion: v1
kind: Service
metadata:
  name: {{ include "ontid-driver.fullname" . }}
  labels:
    {{- include "ontid-driver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ontid-driver.selectorLabels" . | nindent 4 }}
