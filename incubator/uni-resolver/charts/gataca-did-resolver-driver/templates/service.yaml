apiVersion: v1
kind: Service
metadata:
  name: {{ include "gataca-did-resolver-driver.fullname" . }}
  labels:
    {{- include "gataca-did-resolver-driver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gataca-did-resolver-driver.selectorLabels" . | nindent 4 }}
