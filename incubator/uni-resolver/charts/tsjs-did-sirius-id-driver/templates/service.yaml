apiVersion: v1
kind: Service
metadata:
  name: {{ include "tsjs-did-sirius-id-driver.fullname" . }}
  labels:
    {{- include "tsjs-did-sirius-id-driver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tsjs-did-sirius-id-driver.selectorLabels" . | nindent 4 }}
