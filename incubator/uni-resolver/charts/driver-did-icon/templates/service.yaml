apiVersion: v1
kind: Service
metadata:
  name: {{ include "driver-did-icon.fullname" . }}
  labels:
    {{- include "driver-did-icon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "driver-did-icon.selectorLabels" . | nindent 4 }}
