apiVersion: v1
kind: Service
metadata:
  name: {{ include "uni-resolver-did-v1-driver.fullname" . }}
  labels:
    {{- include "uni-resolver-did-v1-driver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "uni-resolver-did-v1-driver.selectorLabels" . | nindent 4 }}
