apiVersion: v1
kind: Service
metadata:
  name: {{ include "driver-did-stack.fullname" . }}
  labels:
    {{- include "driver-did-stack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "driver-did-stack.selectorLabels" . | nindent 4 }}
