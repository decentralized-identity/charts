apiVersion: v1
kind: Service
metadata:
  name: {{ include "driver-did-vaa.fullname" . }}
  labels:
    {{- include "driver-did-vaa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "driver-did-vaa.selectorLabels" . | nindent 4 }}
