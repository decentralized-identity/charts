apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "driver-did-vaa.fullname" . }}-test-connection"
  labels:
    {{- include "driver-did-vaa.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "driver-did-vaa.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
