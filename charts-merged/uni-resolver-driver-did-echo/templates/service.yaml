apiVersion: v1
kind: Service
metadata:
  name: {{ include "uni-resolver-driver-did-echo.fullname" . }}
  labels:
    {{- include "uni-resolver-driver-did-echo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "uni-resolver-driver-did-echo.selectorLabels" . | nindent 4 }}
