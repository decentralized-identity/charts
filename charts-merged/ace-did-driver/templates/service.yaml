apiVersion: v1
kind: Service
metadata:
  name: {{ include "ace-did-driver.fullname" . }}
  labels:
    {{- include "ace-did-driver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ace-did-driver.selectorLabels" . | nindent 4 }}
