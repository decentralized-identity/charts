apiVersion: v1
kind: Service
metadata:
  name: {{ include "kilt-did-driver.fullname" . }}
  labels:
    {{- include "kilt-did-driver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kilt-did-driver.selectorLabels" . | nindent 4 }}
