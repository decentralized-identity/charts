apiVersion: v1
kind: Service
metadata:
  name: {{ include "driver-did-mpg.fullname" . }}
  labels:
    {{- include "driver-did-mpg.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "driver-did-mpg.selectorLabels" . | nindent 4 }}
