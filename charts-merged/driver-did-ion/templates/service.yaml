apiVersion: v1
kind: Service
metadata:
  name: {{ include "driver-did-ion.fullname" . }}
  labels:
    {{- include "driver-did-ion.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "driver-did-ion.selectorLabels" . | nindent 4 }}
