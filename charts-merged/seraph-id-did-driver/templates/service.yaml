apiVersion: v1
kind: Service
metadata:
  name: {{ include "seraph-id-did-driver.fullname" . }}
  labels:
    {{- include "seraph-id-did-driver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "seraph-id-did-driver.selectorLabels" . | nindent 4 }}
