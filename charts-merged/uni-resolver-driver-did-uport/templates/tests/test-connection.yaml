apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "uni-resolver-driver-did-uport.fullname" . }}-test-connection"
  labels:
    {{- include "uni-resolver-driver-did-uport.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "uni-resolver-driver-did-uport.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
