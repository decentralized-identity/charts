apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ontid-driver.fullname" . }}-test-connection"
  labels:
    {{- include "ontid-driver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ontid-driver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
