apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "uni-resolver-driver-did-io.fullname" . }}-test-connection"
  labels:
    {{- include "uni-resolver-driver-did-io.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "uni-resolver-driver-did-io.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
