apiVersion: v1
kind: Service
metadata:
  name: {{ include "driver-dns.fullname" . }}
  labels:
    {{- include "driver-dns.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "driver-dns.selectorLabels" . | nindent 4 }}
