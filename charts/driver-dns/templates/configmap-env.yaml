apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "driver-dns.fullname" . }}-env
  labels:
    app: {{ template "driver-dns.name" . }}
    chart: {{ template "driver-dns.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
{{- if .Values.extraLabels }}
{{ toYaml .Values.extraLabels | indent 4 }}
{{- end }}
data:
{{ toYaml .Values.env | indent 2 }}
