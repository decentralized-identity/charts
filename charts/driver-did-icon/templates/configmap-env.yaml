apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "driver-did-icon.fullname" . }}-env
  labels:
    app: {{ template "driver-did-icon.name" . }}
    chart: {{ template "driver-did-icon.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
{{- if .Values.extraLabels }}
{{ toYaml .Values.extraLabels | indent 4 }}
{{- end }}
data:
{{ toYaml .Values.env | indent 2 }}
