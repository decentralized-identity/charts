apiVersion: v1
kind: Service
metadata:
  name: {{ include "evan-did-driver.fullname" . }}
  labels:
    {{- include "evan-did-driver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "evan-did-driver.selectorLabels" . | nindent 4 }}
