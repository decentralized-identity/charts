apiVersion: v1
kind: Service
metadata:
  name: {{ include "hacera-did-driver.fullname" . }}
  labels:
    {{- include "hacera-did-driver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hacera-did-driver.selectorLabels" . | nindent 4 }}
