apiVersion: v1
kind: Service
metadata:
  name: {{ include "uni-resolver-web.fullname" . }}
  labels:
    {{- include "uni-resolver-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "uni-resolver-web.selectorLabels" . | nindent 4 }}
