apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "uni-resolver-web.fullname" . }}-test-connection"
  labels:
    {{- include "uni-resolver-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "uni-resolver-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
