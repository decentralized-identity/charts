apiVersion: v1
kind: Service
metadata:
  name: {{ include "driver-did-sov.fullname" . }}
  labels:
    {{- include "driver-did-sov.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "driver-did-sov.selectorLabels" . | nindent 4 }}
