apiVersion: v1
kind: Service
metadata:
  name: {{ include "driver-did-trust.fullname" . }}
  labels:
    {{- include "driver-did-trust.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "driver-did-trust.selectorLabels" . | nindent 4 }}
