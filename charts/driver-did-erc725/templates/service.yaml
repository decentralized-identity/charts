apiVersion: v1
kind: Service
metadata:
  name: {{ include "driver-did-erc725.fullname" . }}
  labels:
    {{- include "driver-did-erc725.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "driver-did-erc725.selectorLabels" . | nindent 4 }}
