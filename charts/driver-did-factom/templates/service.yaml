apiVersion: v1
kind: Service
metadata:
  name: {{ include "driver-did-factom.fullname" . }}
  labels:
    {{- include "driver-did-factom.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "driver-did-factom.selectorLabels" . | nindent 4 }}
