apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "driver-did-trustbloc.fullname" . }}-env
  labels:
    app: {{ template "driver-did-trustbloc.name" . }}
    chart: {{ template "driver-did-trustbloc.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
{{- if .Values.extraLabels }}
{{ toYaml .Values.extraLabels | indent 4 }}
{{- end }}
data:
{{ toYaml .Values.env | indent 2 }}
