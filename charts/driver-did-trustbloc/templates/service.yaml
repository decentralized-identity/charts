apiVersion: v1
kind: Service
metadata:
  name: {{ include "driver-did-trustbloc.fullname" . }}
  labels:
    {{- include "driver-did-trustbloc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "driver-did-trustbloc.selectorLabels" . | nindent 4 }}
