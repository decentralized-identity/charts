apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "driver-did-trustbloc.fullname" . }}-test-connection"
  labels:
    {{- include "driver-did-trustbloc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "driver-did-trustbloc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
