apiVersion: v1
kind: Service
metadata:
  name: {{ include "driver-did-btcr.fullname" . }}
  labels:
    {{- include "driver-did-btcr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "driver-did-btcr.selectorLabels" . | nindent 4 }}
