apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "work-did-driver.fullname" . }}-env
  labels:
    app: {{ template "work-did-driver.name" . }}
    chart: {{ template "work-did-driver.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
{{- if .Values.extraLabels }}
{{ toYaml .Values.extraLabels | indent 4 }}
{{- end }}
data:
{{ toYaml .Values.env | indent 2 }}
