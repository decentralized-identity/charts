apiVersion: v1
kind: Service
metadata:
  name: {{ include "work-did-driver.fullname" . }}
  labels:
    {{- include "work-did-driver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "work-did-driver.selectorLabels" . | nindent 4 }}
