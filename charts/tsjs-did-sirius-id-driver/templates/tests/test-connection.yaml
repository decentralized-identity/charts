apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tsjs-did-sirius-id-driver.fullname" . }}-test-connection"
  labels:
    {{- include "tsjs-did-sirius-id-driver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tsjs-did-sirius-id-driver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
