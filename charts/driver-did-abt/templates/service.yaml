apiVersion: v1
kind: Service
metadata:
  name: {{ include "driver-did-abt.fullname" . }}
  labels:
    {{- include "driver-did-abt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "driver-did-abt.selectorLabels" . | nindent 4 }}
