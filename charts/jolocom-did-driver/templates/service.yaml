apiVersion: v1
kind: Service
metadata:
  name: {{ include "jolocom-did-driver.fullname" . }}
  labels:
    {{- include "jolocom-did-driver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jolocom-did-driver.selectorLabels" . | nindent 4 }}
