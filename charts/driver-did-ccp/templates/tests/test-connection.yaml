apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "driver-did-ccp.fullname" . }}-test-connection"
  labels:
    {{- include "driver-did-ccp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "driver-did-ccp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
