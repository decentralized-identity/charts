apiVersion: v1
kind: Service
metadata:
  name: {{ include "uni-resolver-driver-did-uport.fullname" . }}
  labels:
    {{- include "uni-resolver-driver-did-uport.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "uni-resolver-driver-did-uport.selectorLabels" . | nindent 4 }}
