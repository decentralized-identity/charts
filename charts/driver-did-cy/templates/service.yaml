apiVersion: v1
kind: Service
metadata:
  name: {{ include "driver-did-cy.fullname" . }}
  labels:
    {{- include "driver-did-cy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "driver-did-cy.selectorLabels" . | nindent 4 }}
