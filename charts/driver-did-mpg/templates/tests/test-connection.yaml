apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "driver-did-mpg.fullname" . }}-test-connection"
  labels:
    {{- include "driver-did-mpg.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "driver-did-mpg.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
