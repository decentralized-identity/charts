apiVersion: v1
kind: Service
metadata:
  name: {{ include "driver-did-key.fullname" . }}
  labels:
    {{- include "driver-did-key.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "driver-did-key.selectorLabels" . | nindent 4 }}
